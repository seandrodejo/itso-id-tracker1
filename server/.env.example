# ITSO ID Tracker - Backend .env example
# Copy this file to server/.env and fill the values. Do NOT commit server/.env

# Server
PORT=5000

# Database
# Use your local MongoDB URI or a MongoDB Atlas connection string
MONGODB_URI=mongodb://localhost:27017/itso-id-tracker

# Authentication
# Use a long, random, secret string for signing JWTs
JWT_SECRET=your-jwt-secret

# Email transport mode
# simulate (default): no real sending; logs email content to console
# auto: use Gmail OAuth2 if fully configured, otherwise simulate
# password: use Gmail username+app password; requires EMAIL_USER and EMAIL_PASS (RECOMMENDED for simplicity)
EMAIL_TRANSPORT_MODE=password

# Google OAuth (Calendar + Gmail OAuth2 for sending emails without app passwords)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Callback route exposed by the backend for Google OAuth
GOOGLE_REDIRECT_URI=http://localhost:5000/api/auth/google/callback

# Gmail OAuth2 refresh token for the sender account (no 2FA/app password required)
# Generate using Google OAuth Playground with the gmail.send scope
GMAIL_OAUTH_REFRESH_TOKEN=your-gmail-oauth-refresh-token

# Email sender identity (the Gmail address used to send)
EMAIL_USER=your-sender@gmail.com
# Optional: fallback if you still prefer app password (leave empty if using OAuth2)
EMAIL_PASS=

# Frontend origin for CORS and redirects
# If you run Vite on a different port, update accordingly
FRONTEND_URL=http://localhost:5173
